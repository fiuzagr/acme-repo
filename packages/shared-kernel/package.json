{
  "name": "@acme/shared-kernel",
  "version": "1.0.0",
  "description": "",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf .turbo node_modules dist build *.tsbuildinfo",
    "dev": "npm run build -- --watch",
    "lint": "npm run lint:code",
    "lint:code": "TIMING=1 eslint 'src/**/*.{js,jsx,ts,tsx}'",
    "test": "jest",
    "lint:staged": "lint-staged -q"
  },
  "jest": {
    "preset": "@acme/jest-preset/node"
  },
  "devDependencies": {
    "@types/jest": "^29.2.4",
    "jest": "^29.3.1",
    "tsup": "^6.5.0",
    "typescript": "^4.9.4"
  }
}
